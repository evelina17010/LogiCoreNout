<Page x:Class="LC.Pages.Clientrequest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LogiCore.Pages"
      mc:Ignorable="d" 
     d:DesignHeight="800" d:DesignWidth="1000" MaxHeight="1080" MaxWidth="1920" MinHeight="450" MinWidth="800"
      Title="Запрос">

    <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0 40 0 0">
        <Border Style="{StaticResource Clientrequest}">
            <Border Style="{StaticResource Clientrequest1}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Откуда"  Style="{StaticResource ClientStr}" Panel.ZIndex="1" Visibility="{Binding ElementName=txbFrom,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}" Grid.Row="1" />
                    <TextBox x:Name="txbFrom" Style="{StaticResource ClnStr}" Grid.Row="1" />
                    <TextBlock Text="Куда"  Style="{StaticResource ClientStr}" Panel.ZIndex="1" Visibility="{Binding ElementName=txbTo,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}" Grid.Row="2" />
                    <TextBox x:Name="txbTo" Style="{StaticResource ClnStr}" Grid.Row="2" />
                    <TextBlock Text="Масса(кг)"  Style="{StaticResource ClientStr}" Panel.ZIndex="1" Visibility="{Binding ElementName=txbweight,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}" Grid.Row="3" />
                    <TextBox x:Name="txbweight" Style="{StaticResource ClnStr}" Grid.Row="3" />
                    <TextBlock Text="Объём(м3)"  Style="{StaticResource ClientStr}" Panel.ZIndex="1" Visibility="{Binding ElementName=txbvolume,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}" Grid.Row="4" />
                    <TextBox x:Name="txbvolume" Style="{StaticResource ClnStr}" Grid.Row="4" />
                    <TextBlock Text="Описание(Тип груза)"  Style="{StaticResource ClientStr}" Panel.ZIndex="1" Visibility="{Binding ElementName=txbdescription,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}" Grid.Row="0" />
                    <TextBox x:Name="txbdescription" Style="{StaticResource ClnStr}" Grid.Row="0" />
                    <DatePicker Grid.Row="5" Name="dpcargo" Style="{StaticResource ClnDt}"/>
                    <Grid Grid.Row="6">
                        <TextBlock Text="Taриф" Name="tarifftxb" Style="{StaticResource ClientStr}"  Panel.ZIndex="1" Visibility="{Binding ElementName=cmbTariffs,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"  />
                        <ComboBox Name="cmbTariffs" Style="{StaticResource AddRole}"  Width="450" Height="50" Grid.ColumnSpan="2" VerticalAlignment="Center" Foreground="#707070"  HorizontalAlignment="Center" ItemsSource="{Binding Tariffs}"  >
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Description}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Grid.Row="7">
                        <TextBlock x:Name="tbCost"  Style="{StaticResource ClientStr}" Text="Стоимость: -" FontWeight="Bold" Width="400" Foreground="Black"/>
                        <Button  Style="{StaticResource btnadd}" Content="Рассчитать "  Click="CalculateCost_Click" Width="260"/>
                    </StackPanel>
                    <Button Grid.Row="8"  Style="{StaticResource request}" Content="Заказать" Click="ButtonCargo_Click"/>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Page>
